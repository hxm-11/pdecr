# ============================================================
# PD-ECR RAG 依赖清单（可复现，锁定当前实测版本）
# 用法（backend/ 目录，建议在虚拟环境内）：
#     pip install -r requirements-rag.txt
#
# 说明：
#   - 版本号 == 锁定到当前项目实测可用的版本，保证换机可复现。
#   - torch / transformers / tiktoken 由 sentence-transformers、
#     langchain-openai 自动带入，无需手写；如需完全锁定可 `pip freeze`。
#   - Python 3.10+；本地 embedding 与 reranker 模型放在 app/rag/models/ 下离线加载。
# ============================================================

# --- 编排：LangGraph（StateGraph / 节点 / 自检重试）---
langgraph==1.2.7

# --- LangChain 1.x 核心与检索构件 ---
langchain==1.3.11
langchain-core==1.4.8
langchain-community==0.4.2
langchain-classic==1.0.8          # EnsembleRetriever / 重排压缩器（1.x 从 langchain 挪到这里，必须显式装）
langchain-openai==1.3.3           # ChatOpenAI（Azure OpenAI 兼容端点）
langchain-text-splitters==1.1.2   # RecursiveCharacterTextSplitter 切分

# --- 稀疏检索 BM25 ---
rank-bm25==0.2.2                  # BM25Retriever 运行时依赖

# --- Embedding / 向量库 / 重排模型 ---
# 默认 embedding 走 Azure API（text-embedding-3-small，由 langchain-openai 提供），
# 不再强依赖本地大模型。sentence-transformers 仅在以下两种情况需要：
#   1) EMBEDDING_BACKEND=local 回退到本地 sentence-transformers；
#   2) 启用本地 cross-encoder 重排（app/rag/models/bge-reranker-base）。
# 云上/内网若不用本地模型，可整行删掉以省去 torch/transformers 4GB+ 体积。
sentence-transformers==5.5.1      # 可选：本地 embedding 回退 / 本地重排
faiss-cpu==1.14.2
numpy==2.2.6

# --- LLM 客户端 ---
openai==2.41.1

# --- 离线建库文档解析（PDF -> markdown，仅建库阶段用）---
docling==2.103.0
onnxruntime==1.23.2

# --- 配置加载 ---
python-dotenv==1.2.2

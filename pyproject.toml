[project]
name = "full-stack-fastapi-template"
version = "0.1.0"

[tool.uv.workspace]
members = ["backend"]

[dependency-groups]
dev = [
    "zizmor>=1.23.1",
]

github-actions = [
    "smokeshow >=0.5.0",
]
